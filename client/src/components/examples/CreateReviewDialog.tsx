import CreateReviewDialog from '../CreateReviewDialog';

export default function CreateReviewDialogExample() {
  return (
    <div className="p-8 bg-background flex justify-center">
      <CreateReviewDialog />
    </div>
  );
}
